# CI 与本地都用同一套安装口径。
#
# legacy-peer-deps=true 的理由：本插件的 peerDependencies 是**宿主 DSH 在运行时提供的**
# （@deepseek-ai/cordis、dsh-host-webserver、react…），只有 tools/selfcheck.mjs 需要
# 一份 react / react-dom 把面板**真渲染成 HTML**（它按 createRequire(DSH_APP_DIR/package.json)
# 解析这两包；run-all.mjs 把 DSH_APP_DIR 指向仓库根 ⇒ 就解析到这份 devDependencies）。
# npm 7+ 默认**自动安装 peerDependencies**，会顺手去拉一整套 @deepseek-ai/*，
# 等于让一个零外部依赖的插件把 CI 装依赖步骤押在宿主发布状态上（宿主版本一变 CI 就红）。
# 打开这一项后 lockfile 里只剩真正声明的 devDependencies（react + react-dom + 2 个传递依赖）。
legacy-peer-deps=true

# 显式钉公网 registry：lockfile 里写的就是这个源，CI（GitHub runner）必须能取到。
# 本机 $USERPROFILE/.npmrc 指向 npmmirror 镜像，不钉的话 lockfile 会写镜像 URL，
# 换到公网 runner 上取包可能失败 —— 这是"本机能装、CI 装不上"的同类坑。
# 注：这一行只影响**安装测试依赖**这一步；npm test 本身仍然全程不出网。
registry=https://registry.npmjs.org/

# React 版本与宿主对齐（本机 DSH Desktop 内置的是 18.3.1），这样 selfcheck 渲染出的
# 面板与用户实际看到的那份 React 是同一个大版本。
